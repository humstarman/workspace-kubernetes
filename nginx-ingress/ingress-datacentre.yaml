apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: my-k8s-ingress
  namespace: datacentre
spec:
  rules:
  - host: my.k8s.ingress
    http:
      paths:
      - path: /blockchain
        backend:
          serviceName: block-chain-proxy 
          servicePort: 8080
